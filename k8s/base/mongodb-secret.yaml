apiVersion: v1
kind: Secret
metadata:
  name: mongodb-secret
  namespace: llm-services
  labels:
    app.kubernetes.io/part-of: langchain-ollama
    app.kubernetes.io/component: database
type: Opaque
stringData:
  # IMPORTANTE: Actualizar estos valores con tus credenciales reales de MongoDB
  # Para MongoDB sin autenticación, dejar usuario y password vacíos

  # URI completa de conexión a MongoDB
  # Ejemplos:
  #   - MongoDB local: mongodb://localhost:27017
  #   - MongoDB Atlas: mongodb+srv://usuario:password@cluster.mongodb.net
  #   - Con autenticación: mongodb://usuario:password@host:27017/database?authSource=admin
  #   - Dentro del cluster: mongodb://mongodb-service:27017
  MONGODB_URI: "mongodb+srv://eduardocanalsescriva_db_user:hvV5TMyeiAKNueGy@kalimdor.dw0qgi7.mongodb.net"

  # Nombre de la base de datos
  MONGODB_DATABASE: "bocadillos"

  # Usuario y contraseña (opcional, si tu MongoDB requiere autenticación)
  # MONGODB_USERNAME: "tu_usuario"
  # MONGODB_PASSWORD: "tu_password"
