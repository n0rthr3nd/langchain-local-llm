apiVersion: v1
kind: Service
metadata:
  name: langchain-frontend
  namespace: llm-services
  labels:
    app: langchain-frontend
    app.kubernetes.io/part-of: langchain-ollama
spec:
  type: ClusterIP
  selector:
    app: langchain-frontend
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
